<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.waimai.skyserver.mapper.SetmealDishMapper">
    <!-- 查询菜品是否被套餐关联 -->
    <select id="getSetmealIdsByDishIds" resultType="long">
        select setmeal_id from setmeal_dish where dish_id in
        <foreach collection="list" item="dishId" open="(" close=")" separator=",">
            #{dishId}
        </foreach>
    </select>

    <!-- 批量插入套餐和菜品的关联关系 -->
    <insert id="insertBatch" parameterType="list">
        insert into setmeal_dish (setmeal_id, dish_id, name, price, copies)
        values
        <foreach collection="setmealDishes" item="item" separator=",">
            (#{item.setmealId}, #{item.dishId}, #{item.name}, #{item.price}, #{item.copies})
        </foreach>
    </insert>

</mapper>